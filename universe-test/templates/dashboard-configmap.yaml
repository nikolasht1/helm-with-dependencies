apiVersion: v1
kind: ConfigMap
metadata:
  name: blackbox-dashboard
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  blackbox-exporter.json: |-
{{ .Files.Get "dashboards/blackbox-exporter.json" | indent 4 }}
